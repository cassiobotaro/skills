Esse repositório já tem um workspace.dsl que descreve nosso sistema de faturamento. Precisamos evoluir ele: (1) adicionar o ambiente de produção na AWS — o Portal e a API rodam em ECS Fargate (a API com 2 instâncias), o banco é um RDS PostgreSQL, e tem um Application Load Balancer na frente da API; o worker também roda no ECS Fargate; (2) criar um diagrama dinâmico do fluxo 'emissão de fatura': o atendente emite pelo portal, o portal chama a API, a API grava no banco e publica na fila, o worker consome a fila e envia pro ERP; (3) a gente mantém ADRs em docs/adr — vincula eles no workspace. Não mude o que já existe além do necessário.

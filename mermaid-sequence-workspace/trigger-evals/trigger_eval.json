[
  { "query": "draw a sequence diagram of our login flow: browser → BFF → Keycloak → BFF → browser, including the token exchange. put it in markdown for the wiki", "should_trigger": true, "note": "explicit sequence diagram in markdown" },
  { "query": "diagram how the frontend talks to the API and the API talks to the cache and DB when a user loads their dashboard — show the order of calls", "should_trigger": true, "note": "interaction over time, no acronym" },
  { "query": "show what happens step by step when a customer checks out: cart service, inventory check, payment, order creation, confirmation email", "should_trigger": true, "note": "step-by-step flow" },
  { "query": "document this webhook flow as a diagram — Stripe sends us the event, we verify the signature, enqueue it, and ack. show the failure path too if the signature is bad", "should_trigger": true, "note": "webhook flow + failure path" },
  { "query": "i need a sequence diagram for the password reset: user requests reset, we email a token, they submit new password, we validate and update", "should_trigger": true, "note": "explicit sequence diagram" },
  { "query": "desenha um diagrama de sequência mostrando a comunicação entre o app mobile, o gateway e o serviço de notificações no envio de push", "should_trigger": true, "note": "PT, sequence diagram" },
  { "query": "diagram the retry/backoff interaction between our worker and the third-party API when it returns 429s — as a mermaid block", "should_trigger": true, "note": "interaction + mermaid block" },
  { "query": "show the message flow for our async order processing: producer publishes to Kafka, consumer picks it up, calls the fulfillment service, emits an event", "should_trigger": true, "note": "async message flow over time" },
  { "query": "i want to visualize the OAuth authorization code flow with PKCE as a sequence — client, auth server, resource server, all the round trips", "should_trigger": true, "note": "visualize a flow as a sequence" },
  { "query": "create a diagram of the request/response chain when our API gateway authenticates a call, checks rate limits, then forwards to the upstream service", "should_trigger": true, "note": "request/response chain" },

  { "query": "create the C4 container diagram for our platform in workspace.dsl", "should_trigger": false, "note": "belongs to structurizr" },
  { "query": "draw an ER diagram of our orders database in mermaid — tables and relationships", "should_trigger": false, "note": "Mermaid but ER, not sequence — KEY discriminator" },
  { "query": "make a flowchart in mermaid of our deployment decision logic (the if/else branches)", "should_trigger": false, "note": "Mermaid but flowchart, not sequence — KEY discriminator" },
  { "query": "create a gantt chart in mermaid for our Q3 roadmap", "should_trigger": false, "note": "Mermaid but gantt, not sequence" },
  { "query": "write a design doc for the new event pipeline, with the flow of data between services", "should_trigger": false, "note": "belongs to design-doc (mentions 'flow' but wants a doc)" },
  { "query": "document how our microservices are structured and connected at a high level using C4", "should_trigger": false, "note": "belongs to structurizr (static structure)" },
  { "query": "record our decision to use Mermaid for all docs diagrams as an ADR", "should_trigger": false, "note": "mentions Mermaid but it's a decision to record (adr)" },
  { "query": "explain how the OAuth authorization code flow works", "should_trigger": false, "note": "conceptual Q&A: 'explain' not 'diagram' (contrast with the visualize one)" },
  { "query": "draw our team's org chart", "should_trigger": false, "note": "near-miss: a diagram but none of these skills" },
  { "query": "set up mermaid-cli in our build so diagrams render in CI", "should_trigger": false, "note": "near-miss: Mermaid tooling/infra, not authoring a sequence" }
]

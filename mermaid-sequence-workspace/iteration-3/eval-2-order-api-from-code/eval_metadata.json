{
  "eval_id": 2,
  "eval_name": "order-api-from-code",
  "prompt": "Dá uma olhada no código da nossa API de pedidos em /home/cassiobotaro/Projetos/skills/mermaid-sequence-workspace/evals/files/order-api e gera um diagrama de sequência do que acontece quando chega um POST /orders, pra eu colocar no README do serviço. Usa os detalhes reais do código.",
  "assertions": [
    "Output contains a fenced mermaid sequenceDiagram block that parses successfully with mermaid-cli (exit 0)",
    "The real flow from the code is shown: POST /orders -> PayGate POST /v2/charges -> Postgres INSERT -> RabbitMQ order.created publish, with 201 on success",
    "The declined-payment branch from the code is shown (declined -> 402)",
    "Notes/messages carry details actually present in the code (e.g. Authorization: Bearer, Idempotency-Key, card_token/amount/currency payload fields, orders exchange/routing key) and contain nothing absent from the code",
    "The RabbitMQ publish is drawn fire-and-forget/async, matching the code comment that the response does not wait for consumers",
    "Did not ask the user questions that the provided code answers"
  ],
  "expected_output": "Reads the code and produces a fenced ```mermaid block with the real flow: client -> Order API (POST /orders, 201), Order API -> PayGate (POST /v2/charges with Authorization: Bearer and Idempotency-Key, approved/declined), declined -> 402 branch, Postgres INSERT, RabbitMQ order.created published fire-and-forget. Notes carry details actually present in the code; no invented headers/fields; no unnecessary questions back to the user."
}